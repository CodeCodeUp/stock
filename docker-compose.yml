services:
  frontend:
    build:
      context: ./frontend
      args:
        DOCKER_MIRROR: ${DOCKER_MIRROR:-docker.m.daocloud.io}
        NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmmirror.com}
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend
      - data-api
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      args:
        DOCKER_MIRROR: ${DOCKER_MIRROR:-docker.m.daocloud.io}
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://${DB_HOST}:${DB_PORT:-3306}/${DB_NAME:-stock}?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      SPRING_DATASOURCE_USERNAME: ${DB_USER}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      DATA_API_URL: http://data-api:${DATA_API_PORT:-5000}
    restart: unless-stopped

  data-api:
    build:
      context: ./python
      args:
        DOCKER_MIRROR: ${DOCKER_MIRROR:-docker.m.daocloud.io}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.tuna.tsinghua.edu.cn/simple}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-pypi.tuna.tsinghua.edu.cn}
    environment:
      DATA_API_HOST: 0.0.0.0
      DATA_API_PORT: ${DATA_API_PORT:-5000}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT:-3306}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME:-stock}
    restart: unless-stopped

  data-scheduler:
    build:
      context: ./python
      args:
        DOCKER_MIRROR: ${DOCKER_MIRROR:-docker.m.daocloud.io}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.tuna.tsinghua.edu.cn/simple}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-pypi.tuna.tsinghua.edu.cn}
    command: ["python", "scheduler.py"]
    environment:
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT:-3306}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME:-stock}
      SCHEDULER_TIMEZONE: ${SCHEDULER_TIMEZONE:-Asia/Shanghai}
      ENABLE_IMPORTER: ${ENABLE_IMPORTER:-true}
      ENABLE_PRICE_TRACKING: ${ENABLE_PRICE_TRACKING:-true}
    restart: unless-stopped

# Use external network to reach host MySQL, or set DB_HOST to host IP
# If MySQL runs in another compose project, use:
#   networks:
#     default:
#       external:
#         name: <mysql_network_name>
